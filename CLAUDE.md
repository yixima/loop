# CLAUDE.md — 判断の原則（Step1）

このファイルは「AIが迷ったときの判断基準」を固定するためのものです。
**自動化の第一歩はAIを動かすことではなく、AIの判断基準を固定すること**です。

> ここに書かれた原則は、個別のタスク指示より優先されます。
> 原則と指示が矛盾する場合は、作業を止めて人間に確認してください。

---

## 1. 実装の原則

1. **最小の実装から始める。** 動く最小単位を作り、テストを通してから拡張する。「あとで使うかもしれない」機能を先回りして作らない。
2. **仕様を推測で変更しない。** まず既存の仕組みを `rg` / `git log` / ドキュメントで調べる。調べても分からなければ、推測で進めず質問として記録する。
3. **基盤部分に仮のデータを使わない。** モック・ダミー・ハードコードされた戻り値をプロダクションコードに残さない。使う場合はテストコード内に閉じ込め、`TODO` ではなく理由をコメントに書く。
4. **既存のコードスタイルに合わせる。** 命名・コメント密度・ディレクトリ構成は、周囲のコードを読んでから真似る。自分の好みを持ち込まない。
5. **1つの課題では1つのことだけをやる。** 気づいた別の問題は `docs/issues/` に新しい課題ファイルとして切り出し、いま触っているコードには手を入れない。
6. **削除・上書きの前に必ず対象を読む。** `rm -rf`、ファイル全体の上書き、`git reset --hard`、`git push --force` は人間の承認なしに実行しない。

## 2. 検証の原則

7. **「できました」は主張であって事実ではない。** テスト結果という証拠が揃うまで、完了として扱わない（Proof-or-Stop）。
8. **完了を報告する前に必ず `./scripts/verify.sh` を実行する。** 終了コードが 0 でないものを「完了」と呼ばない。
9. **テストを消して緑にしない。** テストのスキップ・削除・無効化・アサーションの緩和で合格させることは禁止。テストが間違っている場合は、その根拠を示して人間に確認する。
10. **失敗したら原因を特定してから直す。** 「たぶんこれだろう」で複数箇所を同時に変えない。再現 → 原因特定 → 最小修正 → 再検証 の順を守る。
11. **同じ失敗を3回繰り返したら止まる。** 修正を3回試して verify が通らなければ、それ以上試行せず、試したこと・分かったこと・仮説を報告して停止する。

## 3. 記憶とドキュメントの原則（Step2）

12. **状態はファイルに書き出す。** コンテキストは必ず失われる。要件定義書・設計書・実装計画書を `docs/` に残し、次に起動したAIが読んで引き継げる状態を維持する。
13. **実装の前に設計書を書く。** 課題 → 要件定義書 → 設計書 → 実装計画書 → 実装、の順序を飛ばさない。
14. **状況が変わったら「記憶」を更新する。** 設計と実装がズレたら、コードだけ直してドキュメントを放置しない。実装計画書のチェックボックスは進捗に合わせて必ず更新する。

## 4. 役割分担の原則（Step3）

15. **自分の宿題を自分で採点しない。** 実装したエージェントがレビューの合否を判定してはいけない。レビューは `reviewer` サブエージェント（別ベンダーのGPT系モデル）に必ず通す。
16. **役割を混ぜない。** 設計は `architect`、実装は `implementer`、レビューは `reviewer`。1つのセッションで役割を掛け持ちしない。

## 5. 停止と人間の関与の原則

17. **ゴールは機械が判定できる形に落とす。** 「ログイン機能を完成させろ」は悪いゴール。「テストが全件通り、lintエラーが0件。5回試して駄目なら状況を報告して止まる」が良いゴール。
18. **本番反映（デプロイ）は人間が判断する。** システム上は反映できる状態でも、人間の承認なしにデプロイしない。ここは意図的に自動化しない工程。
19. **例外は握りつぶさない。** 想定外の状態になったら、リカバリを試みるより先に、何が起きたかを正確に報告する。成功していないものを成功と報告しない。
20. **迷ったら止まる。** 破壊的な操作、外部への送信、認証情報の扱い、スコープ外の変更 — これらで判断に迷ったら、実行せずに人間へ確認する。

## 6. コストの原則

21. **定型作業には安価なモデル、重要な判断には高性能なモデルを充てる。** ループ化すると利用料は増える。ファイル整形・単純な置換にまで高性能モデルを使わない。

---

## このリポジトリの構成

| パス | 役割 |
|---|---|
| `CLAUDE.md` | 判断の原則（このファイル・Step1） |
| `docs/templates/` | 要件定義書・設計書・実装計画書・課題のテンプレート（Step2） |
| `docs/issues/` | 課題ファイル。ループの入り口 |
| `docs/requirements/`, `docs/design/`, `docs/plans/` | AIの外部記憶。課題IDごとに生成される |
| `docs/reviews/` | GPT系モデルによるレビュー結果 |
| `.claude/agents/` | サブエージェント定義（Step3） |
| `.claude/skills/` | 標準手順書＝スキル（Step5）、外側のループ `issue-to-merge` |
| `scripts/verify.sh` | 機械が判定できる合格条件（Step4）。**唯一の停止条件** |
| `scripts/gpt-review.sh` | 別ベンダーモデル（Codex/GPT）へレビューを依頼する |

## 開発コマンド

```bash
./scripts/verify.sh              # lint + typecheck + test（停止条件の判定）
./scripts/verify.sh --quick      # テストのみ
./scripts/gpt-review.sh <base>   # 差分を GPT にレビューさせる
./scripts/loop.sh <issue-file>   # 外側のループをシェルから回す（任意）
```
