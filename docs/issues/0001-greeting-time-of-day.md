# 課題: 時間帯に応じた挨拶を返せるようにする

- **ID**: `0001-greeting-time-of-day`
- **起票日**: 2026-08-21
- **優先度**: medium
- **状態**: done（2026-08-25 人間の判断でマージ。レビュー blocker 2件は `docs/issues/0003-verify-detect-test-tampering.md` へ切り出し）

> これはループの動作確認用のサンプル課題です。
> `./scripts/loop.sh docs/issues/0001-greeting-time-of-day.md` または
> Claude Code 内で `/issue-to-merge 0001-greeting-time-of-day` を実行すると、
> 設計 → 実装 → 別モデルレビュー → 検証 まで自動で流れることを確認できます。
> 本番のプロジェクトを始めるときは、このファイルとサンプル実装ごと削除してください。

## 背景（なぜやるのか）

現在の `greet()` は時刻に関係なく常に「こんにちは」を返す。
社内ツールの通知文で使われており、深夜のバッチ通知にも「こんにちは」と出てしまい、
受け取った人が「これは自動送信か手動送信か」を判断しづらいという声が出ている。
時間帯が文面に現れれば、送信タイミングが一目で分かる。

## ゴール（何が達成されたら終わりか）

`greet()` が、渡された時刻（または現在時刻）に応じて「おはようございます」「こんにちは」「こんばんは」を
使い分けられる状態。既存の呼び出し側は一切変更しなくても、これまでどおり動くこと。

## 受け入れ条件（機械が判定できる形で）

- [x] `greet("山田", hour=5)` 〜 `greet("山田", hour=10)` が `"おはようございます、山田さん"` を返す
- [x] `greet("山田", hour=11)` 〜 `greet("山田", hour=17)` が `"こんにちは、山田さん"` を返す
- [x] `greet("山田", hour=18)` 〜 `greet("山田", hour=4)`（日をまたぐ）が `"こんばんは、山田さん"` を返す
- [x] `hour` が 0〜23 の範囲外なら `ValueError` を送出する
- [x] `hour` を省略した場合は現在時刻を使う（テストでは時刻を注入して検証すること。実時刻に依存するテストを書かない）
- [ ] ~~既存の `test_greeting.py` の5件が **1件も変更されずに** 通り続ける（後方互換）~~
      **取り下げ（2026-08-21 人間の決定）**: この条件は「`hour` を省略した場合は現在時刻を使う」と両立しない。
      既存テストは `greet("山田") == "こんにちは、山田さん"` を期待しており、現在時刻を使うと
      11〜17時台にしか verify が緑にならない。後方互換の意味を下記2条件に置き換える。
- [x] `tests/test_greeting.py` の書き換えは、時刻を明示注入する2件（`greet("山田", hour=12)` /
      `greet("  山田  ", hour=12)`）に限定する。例外系の3件は変更しない
- [x] `greet("山田")` が例外を出さずに動く（シグネチャ後方互換）
- [x] `name` の例外仕様（空文字 `ValueError` / 非str `TypeError`）が変わらない
- [x] `greet("山田", 5)` は `TypeError`（`hour` はキーワード専用）
- [x] `hour` が int 以外（`"5"` / `5.0` / `True`）なら `TypeError`（bool も拒否）
- [x] `./scripts/verify.sh` が終了コード 0 で完了する

## スコープ外（やらないこと）

- タイムゾーンの扱い（ローカル時刻のみを対象とする）
- 日本語以外へのローカライズ
- `greet()` 以外の関数の追加・変更
- CLI やAPI の追加

## 制約・前提

- 対象ファイルは `src/loopdemo/greeting.py` と `tests/` のみ
- 既存の引数 `name` のシグネチャと例外仕様（空文字で `ValueError`、非str で `TypeError`）は変えない
- 境界値（4/5、10/11、17/18）はすべてテストで押さえること

## 停止条件

- 受け入れ条件をすべて満たし `./scripts/verify.sh` が通ったら完了。
- 修正を **5回** 試して通らなければ、状況を報告して停止する。
- 判断に迷う仕様変更が必要になったら、実装せず質問して停止する。
